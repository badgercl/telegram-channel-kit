<?php
class TelegramBotConfig{
static $show_output = TRUE; #whether the post bot shows good results (errors will always be printed)
static $bot_token = ""; #get it from @BotFather
static $channel_id = ""; #Check the readme on who to get it
static $parse_mode = "HTML";
}
